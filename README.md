# orm_CRUD_operation_one_to_table


This Django project showcases ORM (Object Relational Mapping) operations with a one-to-many relationship between two tables. It demonstrates how to insert, retrieve, and display data using both backend (Django admin) and frontend (HTML forms with views and templates). The project highlights practical CRUD functionality, template rendering, and database interaction using Django’s ORM.
